name: CEO
role: strategist
label: ceo
escalates_to: board

required_tools:
  - github                              # GitHub issue management (if provider: github)
  - mcp__claude_ai_Linear               # Linear issue management (if provider: linear)

runtime:
  model: sonnet                         # Strategic thinking needs good reasoning, not max
  thinking_effort: high
  max_turns: 100
  enable_chrome: false
  timeout: 0
  extra_args: []

