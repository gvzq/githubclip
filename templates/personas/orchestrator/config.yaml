name: Orchestrator
role: orchestrator
label: null
is_default: true
escalates_to: ceo

required_tools:
  - github                              # GitHub MCP tools (if provider: github)
  - mcp__claude_ai_Linear               # Linear MCP tools (if provider: linear)

runtime:
  model: haiku                          # Routing is mechanical – cheapest model works
  thinking_effort: low
  max_turns: 50
  enable_chrome: false
  timeout: 0
  extra_args: []

